import __buffer_polyfill from 'vite-plugin-node-polyfills/shims/buffer'
globalThis.Buffer = globalThis.Buffer || __buffer_polyfill
import __global_polyfill from 'vite-plugin-node-polyfills/shims/global'
globalThis.global = globalThis.global || __global_polyfill
import __process_polyfill from 'vite-plugin-node-polyfills/shims/process'
globalThis.process = globalThis.process || __process_polyfill

import {
  index_browser_esm_exports,
  init_index_browser_esm
} from "./chunk-XWR6224S.js";
import "./chunk-XXLRWMFW.js";
import "./chunk-26YQ5DDQ.js";
import "./chunk-J3K75QGL.js";
import "./chunk-W53XGSBD.js";
import "./chunk-FQ2RZEFG.js";
import "./chunk-OE2KPVLC.js";
import "./chunk-KK4BMKHV.js";
import {
  __commonJS,
  __toCommonJS,
  __toESM,
  require_dist,
  require_dist2,
  require_dist3
} from "./chunk-DHEA4WJK.js";

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/constants.js
var require_constants = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/constants.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PERMISSION_PROGRAM_ID = exports.MAGIC_CONTEXT_ID = exports.MAGIC_PROGRAM_ID = exports.DELEGATION_PROGRAM_ID = void 0;
    var web3_js_1 = (init_index_browser_esm(), __toCommonJS(index_browser_esm_exports));
    exports.DELEGATION_PROGRAM_ID = new web3_js_1.PublicKey("DELeGGvXpWV2fqJUhqcF5ZSYMS4JTLjteaAMARRSaeSh");
    exports.MAGIC_PROGRAM_ID = new web3_js_1.PublicKey("Magic11111111111111111111111111111111111111");
    exports.MAGIC_CONTEXT_ID = new web3_js_1.PublicKey("MagicContext1111111111111111111111111111111");
    exports.PERMISSION_PROGRAM_ID = new web3_js_1.PublicKey("BTWAqWNBmF2TboMh3fxMJfgR16xGHYD7Kgr2dPwbRPBi");
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/pda.js
var require_pda = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/pda.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.delegationRecordPdaFromDelegatedAccount = delegationRecordPdaFromDelegatedAccount;
    exports.delegationMetadataPdaFromDelegatedAccount = delegationMetadataPdaFromDelegatedAccount;
    exports.delegateBufferPdaFromDelegatedAccountAndOwnerProgram = delegateBufferPdaFromDelegatedAccountAndOwnerProgram;
    exports.escrowPdaFromEscrowAuthority = escrowPdaFromEscrowAuthority;
    exports.undelegateBufferPdaFromDelegatedAccount = undelegateBufferPdaFromDelegatedAccount;
    exports.feesVaultPda = feesVaultPda;
    exports.validatorFeesVaultPdaFromValidator = validatorFeesVaultPdaFromValidator;
    exports.commitStatePdaFromDelegatedAccount = commitStatePdaFromDelegatedAccount;
    exports.commitRecordPdaFromDelegatedAccount = commitRecordPdaFromDelegatedAccount;
    exports.permissionPdaFromAccount = permissionPdaFromAccount;
    exports.groupPdaFromId = groupPdaFromId;
    var web3_js_1 = (init_index_browser_esm(), __toCommonJS(index_browser_esm_exports));
    var constants_js_1 = require_constants();
    function delegationRecordPdaFromDelegatedAccount(delegatedAccount) {
      return web3_js_1.PublicKey.findProgramAddressSync([Buffer.from("delegation"), delegatedAccount.toBytes()], constants_js_1.DELEGATION_PROGRAM_ID)[0];
    }
    function delegationMetadataPdaFromDelegatedAccount(delegatedAccount) {
      return web3_js_1.PublicKey.findProgramAddressSync([Buffer.from("delegation-metadata"), delegatedAccount.toBytes()], constants_js_1.DELEGATION_PROGRAM_ID)[0];
    }
    function delegateBufferPdaFromDelegatedAccountAndOwnerProgram(delegatedAccount, ownerProgramId) {
      return web3_js_1.PublicKey.findProgramAddressSync([Buffer.from("buffer"), delegatedAccount.toBytes()], ownerProgramId)[0];
    }
    function escrowPdaFromEscrowAuthority(escrowAuthority, index = 255) {
      if (index < 0 || index > 255) {
        throw new Error("Index must be between 0 and 255");
      }
      return web3_js_1.PublicKey.findProgramAddressSync([Buffer.from("balance"), escrowAuthority.toBytes(), Buffer.from([index])], constants_js_1.DELEGATION_PROGRAM_ID)[0];
    }
    function undelegateBufferPdaFromDelegatedAccount(delegatedAccount) {
      return web3_js_1.PublicKey.findProgramAddressSync([Buffer.from("undelegate-buffer"), delegatedAccount.toBytes()], constants_js_1.DELEGATION_PROGRAM_ID)[0];
    }
    function feesVaultPda() {
      return web3_js_1.PublicKey.findProgramAddressSync([Buffer.from("fees-vault")], constants_js_1.DELEGATION_PROGRAM_ID)[0];
    }
    function validatorFeesVaultPdaFromValidator(validator) {
      return web3_js_1.PublicKey.findProgramAddressSync([Buffer.from("v-fees-vault"), validator.toBytes()], constants_js_1.DELEGATION_PROGRAM_ID)[0];
    }
    function commitStatePdaFromDelegatedAccount(delegatedAccount) {
      return web3_js_1.PublicKey.findProgramAddressSync([Buffer.from("state-diff"), delegatedAccount.toBytes()], constants_js_1.DELEGATION_PROGRAM_ID)[0];
    }
    function commitRecordPdaFromDelegatedAccount(delegatedAccount) {
      return web3_js_1.PublicKey.findProgramAddressSync([Buffer.from("commit-state-record"), delegatedAccount.toBytes()], constants_js_1.DELEGATION_PROGRAM_ID)[0];
    }
    var PERMISSION_SEED = Buffer.from("permission:");
    var GROUP_SEED = Buffer.from("group:");
    function permissionPdaFromAccount(account) {
      return web3_js_1.PublicKey.findProgramAddressSync([PERMISSION_SEED, account.toBuffer()], constants_js_1.PERMISSION_PROGRAM_ID)[0];
    }
    function groupPdaFromId(id) {
      return web3_js_1.PublicKey.findProgramAddressSync([GROUP_SEED, id.toBuffer()], constants_js_1.PERMISSION_PROGRAM_ID)[0];
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/utils.js
var require_utils = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/utils.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.GetCommitmentSignature = GetCommitmentSignature;
    async function GetCommitmentSignature(transactionSignature, ephemeralConnection) {
      const txSchedulingSgn = await ephemeralConnection.getTransaction(transactionSignature, { maxSupportedTransactionVersion: 0 });
      if ((txSchedulingSgn == null ? void 0 : txSchedulingSgn.meta) == null) {
        throw new Error("Transaction not found or meta is null");
      }
      const scheduledCommitSgn = parseScheduleCommitsLogsMessage(txSchedulingSgn.meta.logMessages ?? []);
      if (scheduledCommitSgn == null) {
        throw new Error("ScheduledCommitSent signature not found");
      }
      const latestBlockhash = await ephemeralConnection.getLatestBlockhash();
      await ephemeralConnection.confirmTransaction({
        signature: scheduledCommitSgn,
        ...latestBlockhash
      });
      const txCommitInfo = await ephemeralConnection.getTransaction(scheduledCommitSgn, { maxSupportedTransactionVersion: 0 });
      if ((txCommitInfo == null ? void 0 : txCommitInfo.meta) == null) {
        throw new Error("Transaction not found or meta is null");
      }
      const commitSignature = parseCommitsLogsMessage(txCommitInfo.meta.logMessages ?? []);
      if (commitSignature == null) {
        throw new Error("Unable to find Commitment signature");
      }
      return commitSignature;
    }
    function parseScheduleCommitsLogsMessage(logMessages) {
      for (const message of logMessages) {
        const signaturePrefix = "ScheduledCommitSent signature: ";
        if (message.includes(signaturePrefix)) {
          return message.split(signaturePrefix)[1];
        }
      }
      return null;
    }
    function parseCommitsLogsMessage(logMessages) {
      for (const message of logMessages) {
        const signaturePrefix = "ScheduledCommitSent signature[0]: ";
        if (message.includes(signaturePrefix)) {
          return message.split(signaturePrefix)[1];
        }
      }
      return null;
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/resolver.js
var require_resolver = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/resolver.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Resolver = void 0;
    var web3_js_1 = (init_index_browser_esm(), __toCommonJS(index_browser_esm_exports));
    var constants_js_1 = require_constants();
    var DelegationStatus;
    (function(DelegationStatus2) {
      DelegationStatus2[DelegationStatus2["Delegated"] = 0] = "Delegated";
      DelegationStatus2[DelegationStatus2["Undelegated"] = 1] = "Undelegated";
    })(DelegationStatus || (DelegationStatus = {}));
    var Resolver = class {
      constructor(config, routes) {
        this.routes = /* @__PURE__ */ new Map();
        this.delegations = /* @__PURE__ */ new Map();
        this.subs = /* @__PURE__ */ new Set();
        this.chain = new web3_js_1.Connection(config.chain);
        this.ws = new web3_js_1.Connection(config.websocket);
        this.routes = new Map([...routes.entries()].map(([k, v]) => [k, new web3_js_1.Connection(v)]));
      }
      async trackAccount(pubkey) {
        const pubkeyStr = pubkey.toString();
        if (this.delegations.has(pubkeyStr)) {
          const record = this.delegations.get(pubkeyStr);
          if (record !== void 0) {
            return record;
          }
          throw new Error(`Expected a delegation record for ${pubkeyStr}, but found undefined.`);
        }
        const seed = new TextEncoder().encode("delegation");
        const seeds = [seed, pubkey.toBytes()];
        const [delegationRecord] = web3_js_1.PublicKey.findProgramAddressSync(seeds, constants_js_1.DELEGATION_PROGRAM_ID);
        const id = this.ws.onAccountChange(delegationRecord, (acc) => this.updateStatus(acc, pubkey), "confirmed");
        this.subs.add(id);
        const accountInfo = await this.chain.getAccountInfo(delegationRecord, "confirmed");
        return this.updateStatus(accountInfo, pubkey);
      }
      async resolve(pubkey) {
        let record = this.delegations.get(pubkey.toString());
        if (!record) {
          record = await this.trackAccount(pubkey);
        }
        return record.status === DelegationStatus.Delegated ? this.routes.get(record.validator.toString()) : this.chain;
      }
      async resolveForTransaction(tx) {
        const validators = /* @__PURE__ */ new Set();
        for (const { pubkey, isWritable } of tx.instructions.flatMap((i) => i.keys)) {
          if (!isWritable)
            continue;
          const record = await this.trackAccount(pubkey);
          if (record.status === DelegationStatus.Delegated) {
            validators.add(record.validator.toString());
          }
        }
        const vs = [...validators];
        return vs.length === 1 ? this.routes.get(vs[0]) : validators.size === 0 ? this.chain : void 0;
      }
      async terminate() {
        await Promise.all([...this.subs].map(async (sub) => this.ws.removeAccountChangeListener(sub)));
      }
      updateStatus(account, pubkey) {
        const isDelegated = account !== null && account.owner.equals(constants_js_1.DELEGATION_PROGRAM_ID) && account.lamports !== 0;
        const record = isDelegated ? {
          status: DelegationStatus.Delegated,
          validator: new web3_js_1.PublicKey(account.data.subarray(8, 40))
        } : { status: DelegationStatus.Undelegated };
        this.delegations.set(pubkey.toString(), record);
        return record;
      }
    };
    exports.Resolver = Resolver;
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/magic-router.js
var require_magic_router = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/magic-router.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ConnectionMagicRouter = void 0;
    exports.getWritableAccounts = getWritableAccounts;
    var web3_js_1 = (init_index_browser_esm(), __toCommonJS(index_browser_esm_exports));
    function getWritableAccounts(transaction) {
      const writableAccounts = /* @__PURE__ */ new Set();
      if (transaction.feePayer) {
        writableAccounts.add(transaction.feePayer.toBase58());
      }
      for (const instruction of transaction.instructions) {
        for (const key of instruction.keys) {
          if (key.isWritable) {
            writableAccounts.add(key.pubkey.toBase58());
          }
        }
      }
      return Array.from(writableAccounts);
    }
    var ConnectionMagicRouter = class extends web3_js_1.Connection {
      async getClosestValidator() {
        var _a;
        const response = await fetch(this.rpcEndpoint, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            jsonrpc: "2.0",
            id: 1,
            method: "getIdentity",
            params: []
          })
        });
        const identityData = (_a = await response.json()) == null ? void 0 : _a.result;
        if (identityData === null || identityData.identity === void 0) {
          throw new Error("Invalid response");
        }
        return identityData;
      }
      async getDelegationStatus(account) {
        const accountAddress = typeof account === "string" ? account : account.toBase58();
        const response = await fetch(`${this.rpcEndpoint}/getDelegationStatus`, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            jsonrpc: "2.0",
            id: 1,
            method: "getDelegationStatus",
            params: [accountAddress]
          })
        });
        return (await response.json()).result;
      }
      async getLatestBlockhashForTransaction(transaction, options) {
        const writableAccounts = getWritableAccounts(transaction);
        const blockHashResponse = await fetch(this.rpcEndpoint, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            jsonrpc: "2.0",
            id: 1,
            method: "getBlockhashForAccounts",
            params: [writableAccounts]
          })
        });
        const blockHashData = await blockHashResponse.json();
        return blockHashData.result;
      }
      async prepareTransaction(transaction, options) {
        const blockHashData = await this.getLatestBlockhashForTransaction(transaction, options);
        transaction.recentBlockhash = blockHashData.blockhash;
        return transaction;
      }
      async sendTransaction(transaction, signersOrOptions, options) {
        if (transaction instanceof web3_js_1.Transaction) {
          const latestBlockhash = await this.getLatestBlockhashForTransaction(transaction);
          transaction.recentBlockhash = latestBlockhash.blockhash;
          transaction.lastValidBlockHeight = latestBlockhash.lastValidBlockHeight;
          if (Array.isArray(signersOrOptions)) {
            transaction.sign(...signersOrOptions);
          }
          const wireTx = transaction.serialize();
          return this.sendRawTransaction(wireTx, options);
        } else {
          return super.sendTransaction(transaction, signersOrOptions);
        }
      }
      async sendAndConfirmTransaction(transaction, signers, options) {
        const signature = await this.sendTransaction(transaction, signers, options);
        let status;
        const { recentBlockhash, lastValidBlockHeight, minNonceContextSlot, nonceInfo } = transaction;
        if (recentBlockhash !== void 0 && lastValidBlockHeight !== void 0) {
          status = (await this.confirmTransaction({
            abortSignal: options == null ? void 0 : options.abortSignal,
            signature,
            blockhash: recentBlockhash,
            lastValidBlockHeight
          }, options == null ? void 0 : options.commitment)).value;
        } else if (minNonceContextSlot !== void 0 && nonceInfo !== void 0) {
          const { nonceInstruction } = nonceInfo;
          const nonceAccountPubkey = nonceInstruction.keys[0].pubkey;
          status = (await this.confirmTransaction({
            abortSignal: options == null ? void 0 : options.abortSignal,
            minContextSlot: minNonceContextSlot,
            nonceAccountPubkey,
            nonceValue: nonceInfo.nonce,
            signature
          }, options == null ? void 0 : options.commitment)).value;
        } else {
          status = (await this.confirmTransaction(signature, options == null ? void 0 : options.commitment)).value;
        }
        if (status.err != null) {
          throw new web3_js_1.SendTransactionError({
            action: "send",
            signature,
            transactionMessage: `Status: (${JSON.stringify(status)})`
          });
        }
        return signature;
      }
    };
    exports.ConnectionMagicRouter = ConnectionMagicRouter;
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/delegation-program/delegate.js
var require_delegate = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/delegation-program/delegate.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createDelegateInstruction = createDelegateInstruction;
    exports.serializeDelegateInstructionData = serializeDelegateInstructionData;
    var web3_js_1 = (init_index_browser_esm(), __toCommonJS(index_browser_esm_exports));
    var constants_1 = require_constants();
    var pda_1 = require_pda();
    function createDelegateInstruction(accounts, args) {
      const delegateBuffer = (0, pda_1.delegateBufferPdaFromDelegatedAccountAndOwnerProgram)(accounts.delegatedAccount, accounts.ownerProgram);
      const delegationRecord = (0, pda_1.delegationRecordPdaFromDelegatedAccount)(accounts.delegatedAccount);
      const delegationMetadata = (0, pda_1.delegationMetadataPdaFromDelegatedAccount)(accounts.delegatedAccount);
      const keys = [
        { pubkey: accounts.payer, isWritable: true, isSigner: true },
        { pubkey: accounts.delegatedAccount, isWritable: true, isSigner: true },
        { pubkey: accounts.ownerProgram, isWritable: false, isSigner: false },
        { pubkey: delegateBuffer, isWritable: true, isSigner: false },
        { pubkey: delegationRecord, isWritable: true, isSigner: false },
        { pubkey: delegationMetadata, isWritable: true, isSigner: false },
        { pubkey: web3_js_1.SystemProgram.programId, isWritable: false, isSigner: false }
      ];
      const instructionData = serializeDelegateInstructionData({
        validator: accounts.validator,
        ...args
      });
      return new web3_js_1.TransactionInstruction({
        programId: constants_1.DELEGATION_PROGRAM_ID,
        keys,
        data: instructionData
      });
    }
    function serializeDelegateInstructionData(args) {
      const delegateInstructionDiscriminator = [0, 0, 0, 0, 0, 0, 0, 0];
      const commitFrequencyMs = (args == null ? void 0 : args.commitFrequencyMs) ?? 4294967295;
      const seeds = (args == null ? void 0 : args.seeds) ?? [];
      const validator = args == null ? void 0 : args.validator;
      const buffer = Buffer.alloc(1024);
      let offset = 0;
      for (let i = 0; i < 8; i++) {
        buffer[offset++] = delegateInstructionDiscriminator[i];
      }
      buffer.writeUInt32LE(commitFrequencyMs, offset);
      offset += 4;
      buffer.writeUInt32LE(seeds.length, offset);
      offset += 4;
      for (const seed of seeds) {
        buffer.writeUInt32LE(seed.length, offset);
        offset += 4;
        buffer.set(seed, offset);
        offset += seed.length;
      }
      if (validator) {
        buffer[offset++] = 1;
        buffer.set(validator.toBuffer(), offset);
        offset += 32;
      } else {
        buffer[offset++] = 0;
      }
      return buffer.subarray(0, offset);
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/delegation-program/topUpEphemeralBalance.js
var require_topUpEphemeralBalance = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/delegation-program/topUpEphemeralBalance.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createTopUpEscrowInstruction = createTopUpEscrowInstruction;
    exports.serializeTopUpEphemeralBalanceInstructionData = serializeTopUpEphemeralBalanceInstructionData;
    var web3_js_1 = (init_index_browser_esm(), __toCommonJS(index_browser_esm_exports));
    var constants_1 = require_constants();
    function createTopUpEscrowInstruction(escrow, escrowAuthority, payer, amount, index) {
      const keys = [
        { pubkey: payer, isWritable: true, isSigner: true },
        { pubkey: escrowAuthority, isWritable: false, isSigner: false },
        { pubkey: escrow, isWritable: true, isSigner: false },
        { pubkey: web3_js_1.SystemProgram.programId, isWritable: false, isSigner: false }
      ];
      const instructionData = serializeTopUpEphemeralBalanceInstructionData({
        amount: BigInt(amount),
        index: index ?? 255
      });
      return new web3_js_1.TransactionInstruction({
        programId: constants_1.DELEGATION_PROGRAM_ID,
        keys,
        data: instructionData
      });
    }
    function serializeTopUpEphemeralBalanceInstructionData(args) {
      const discriminator = [9, 0, 0, 0, 0, 0, 0, 0];
      const buffer = Buffer.alloc(17);
      let offset = 0;
      for (let i = 0; i < 8; i++) {
        buffer[offset++] = discriminator[i];
      }
      buffer.writeBigUInt64LE(args.amount, offset);
      offset += 8;
      buffer[offset] = args.index;
      return buffer;
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/delegation-program/closeEphemeralBalance.js
var require_closeEphemeralBalance = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/delegation-program/closeEphemeralBalance.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createCloseEscrowInstruction = createCloseEscrowInstruction;
    exports.serializeCloseEphemeralBalanceInstructionData = serializeCloseEphemeralBalanceInstructionData;
    var web3_js_1 = (init_index_browser_esm(), __toCommonJS(index_browser_esm_exports));
    var constants_1 = require_constants();
    function createCloseEscrowInstruction(escrow, escrowAuthority, index) {
      const keys = [
        { pubkey: escrowAuthority, isWritable: false, isSigner: true },
        { pubkey: escrow, isWritable: true, isSigner: false },
        { pubkey: web3_js_1.SystemProgram.programId, isWritable: false, isSigner: false }
      ];
      const instructionData = serializeCloseEphemeralBalanceInstructionData({
        index: index ?? 255
      });
      return new web3_js_1.TransactionInstruction({
        programId: constants_1.DELEGATION_PROGRAM_ID,
        keys,
        data: instructionData
      });
    }
    function serializeCloseEphemeralBalanceInstructionData(args) {
      const discriminator = [11, 0, 0, 0, 0, 0, 0, 0];
      const buffer = Buffer.alloc(9);
      let offset = 0;
      for (let i = 0; i < 8; i++) {
        buffer[offset++] = discriminator[i];
      }
      buffer[offset] = args.index;
      return buffer;
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/delegation-program/index.js
var require_delegation_program = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/delegation-program/index.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createCloseEscrowInstruction = exports.createTopUpEscrowInstruction = exports.createDelegateInstruction = void 0;
    var delegate_1 = require_delegate();
    Object.defineProperty(exports, "createDelegateInstruction", { enumerable: true, get: function() {
      return delegate_1.createDelegateInstruction;
    } });
    var topUpEphemeralBalance_1 = require_topUpEphemeralBalance();
    Object.defineProperty(exports, "createTopUpEscrowInstruction", { enumerable: true, get: function() {
      return topUpEphemeralBalance_1.createTopUpEscrowInstruction;
    } });
    var closeEphemeralBalance_1 = require_closeEphemeralBalance();
    Object.defineProperty(exports, "createCloseEscrowInstruction", { enumerable: true, get: function() {
      return closeEphemeralBalance_1.createCloseEscrowInstruction;
    } });
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/magic-program/scheduleCommit.js
var require_scheduleCommit = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/magic-program/scheduleCommit.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createCommitInstruction = createCommitInstruction;
    var web3_js_1 = (init_index_browser_esm(), __toCommonJS(index_browser_esm_exports));
    var constants_1 = require_constants();
    function createCommitInstruction(payer, accountsToCommit) {
      const accounts = [
        { pubkey: payer, isSigner: true, isWritable: true },
        { pubkey: constants_1.MAGIC_CONTEXT_ID, isSigner: false, isWritable: true },
        ...accountsToCommit.map((account) => ({
          pubkey: account,
          isSigner: false,
          isWritable: false
        }))
      ];
      const data = Buffer.alloc(4);
      data.writeUInt32LE(1, 0);
      return new web3_js_1.TransactionInstruction({
        keys: accounts,
        programId: constants_1.MAGIC_PROGRAM_ID,
        data
      });
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/magic-program/scheduleCommitAndUndelegate.js
var require_scheduleCommitAndUndelegate = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/magic-program/scheduleCommitAndUndelegate.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createCommitAndUndelegateInstruction = createCommitAndUndelegateInstruction;
    var web3_js_1 = (init_index_browser_esm(), __toCommonJS(index_browser_esm_exports));
    var constants_1 = require_constants();
    function createCommitAndUndelegateInstruction(payer, accountsToCommitAndUndelegate) {
      const accounts = [
        { pubkey: payer, isSigner: true, isWritable: true },
        { pubkey: constants_1.MAGIC_CONTEXT_ID, isSigner: false, isWritable: true },
        ...accountsToCommitAndUndelegate.map((account) => ({
          pubkey: account,
          isSigner: false,
          isWritable: false
        }))
      ];
      const data = Buffer.alloc(4);
      data.writeUInt32LE(2, 0);
      return new web3_js_1.TransactionInstruction({
        keys: accounts,
        programId: constants_1.MAGIC_PROGRAM_ID,
        data
      });
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/magic-program/index.js
var require_magic_program = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/magic-program/index.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createCommitAndUndelegateInstruction = exports.createCommitInstruction = void 0;
    var scheduleCommit_1 = require_scheduleCommit();
    Object.defineProperty(exports, "createCommitInstruction", { enumerable: true, get: function() {
      return scheduleCommit_1.createCommitInstruction;
    } });
    var scheduleCommitAndUndelegate_1 = require_scheduleCommitAndUndelegate();
    Object.defineProperty(exports, "createCommitAndUndelegateInstruction", { enumerable: true, get: function() {
      return scheduleCommitAndUndelegate_1.createCommitAndUndelegateInstruction;
    } });
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/permission-program/createGroup.js
var require_createGroup = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/permission-program/createGroup.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createCreateGroupInstruction = createCreateGroupInstruction;
    exports.serializeCreateGroupInstructionData = serializeCreateGroupInstructionData;
    var web3_js_1 = (init_index_browser_esm(), __toCommonJS(index_browser_esm_exports));
    var constants_1 = require_constants();
    function createCreateGroupInstruction(accounts, args) {
      const keys = [
        { pubkey: accounts.group, isWritable: true, isSigner: false },
        { pubkey: accounts.payer, isWritable: true, isSigner: true },
        { pubkey: web3_js_1.SystemProgram.programId, isWritable: false, isSigner: false }
      ];
      const instructionData = serializeCreateGroupInstructionData(args);
      return new web3_js_1.TransactionInstruction({
        programId: constants_1.PERMISSION_PROGRAM_ID,
        keys,
        data: instructionData
      });
    }
    function serializeCreateGroupInstructionData(args) {
      const discriminator = 0;
      const buffer = Buffer.alloc(1e4);
      let offset = 0;
      buffer[offset++] = discriminator;
      buffer.set(args.id.toBuffer(), offset);
      offset += 32;
      buffer.writeUInt32LE(args.members.length, offset);
      offset += 4;
      for (const member of args.members) {
        buffer.set(member.toBuffer(), offset);
        offset += 32;
      }
      return buffer.subarray(0, offset);
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/permission-program/createPermission.js
var require_createPermission = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/permission-program/createPermission.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createCreatePermissionInstruction = createCreatePermissionInstruction;
    exports.serializeCreatePermissionInstructionData = serializeCreatePermissionInstructionData;
    var web3_js_1 = (init_index_browser_esm(), __toCommonJS(index_browser_esm_exports));
    var constants_1 = require_constants();
    function createCreatePermissionInstruction(accounts, args) {
      const keys = [
        { pubkey: accounts.permission, isWritable: true, isSigner: false },
        { pubkey: accounts.delegatedAccount, isWritable: false, isSigner: true },
        { pubkey: accounts.group, isWritable: false, isSigner: false },
        { pubkey: accounts.payer, isWritable: true, isSigner: true },
        { pubkey: web3_js_1.SystemProgram.programId, isWritable: false, isSigner: false }
      ];
      const instructionData = serializeCreatePermissionInstructionData(args);
      return new web3_js_1.TransactionInstruction({
        programId: constants_1.PERMISSION_PROGRAM_ID,
        keys,
        data: instructionData
      });
    }
    function serializeCreatePermissionInstructionData(args) {
      const discriminator = 1;
      const buffer = Buffer.alloc(1);
      let offset = 0;
      buffer[offset++] = discriminator;
      return buffer.subarray(0, offset);
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/permission-program/updatePermission.js
var require_updatePermission = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/permission-program/updatePermission.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createUpdatePermissionInstruction = createUpdatePermissionInstruction;
    exports.serializeUpdatePermissionInstructionData = serializeUpdatePermissionInstructionData;
    var web3_js_1 = (init_index_browser_esm(), __toCommonJS(index_browser_esm_exports));
    var constants_1 = require_constants();
    function createUpdatePermissionInstruction(accounts, args) {
      const keys = [
        { pubkey: accounts.permission, isWritable: true, isSigner: false },
        { pubkey: accounts.delegatedAccount, isWritable: false, isSigner: true },
        { pubkey: accounts.group, isWritable: false, isSigner: false }
      ];
      const instructionData = serializeUpdatePermissionInstructionData(args);
      return new web3_js_1.TransactionInstruction({
        programId: constants_1.PERMISSION_PROGRAM_ID,
        keys,
        data: instructionData
      });
    }
    function serializeUpdatePermissionInstructionData(args) {
      const discriminator = 2;
      const buffer = Buffer.alloc(1);
      let offset = 0;
      buffer[offset++] = discriminator;
      return buffer.subarray(0, offset);
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/permission-program/index.js
var require_permission_program = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/permission-program/index.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createUpdatePermissionInstruction = exports.createCreatePermissionInstruction = exports.createCreateGroupInstruction = void 0;
    var createGroup_1 = require_createGroup();
    Object.defineProperty(exports, "createCreateGroupInstruction", { enumerable: true, get: function() {
      return createGroup_1.createCreateGroupInstruction;
    } });
    var createPermission_1 = require_createPermission();
    Object.defineProperty(exports, "createCreatePermissionInstruction", { enumerable: true, get: function() {
      return createPermission_1.createCreatePermissionInstruction;
    } });
    var updatePermission_1 = require_updatePermission();
    Object.defineProperty(exports, "createUpdatePermissionInstruction", { enumerable: true, get: function() {
      return updatePermission_1.createUpdatePermissionInstruction;
    } });
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/index.js
var require_instructions = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/instructions/index.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar(require_delegation_program(), exports);
    __exportStar(require_magic_program(), exports);
    __exportStar(require_permission_program(), exports);
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/access-control/auth.js
var require_auth = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/access-control/auth.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SESSION_DURATION = void 0;
    exports.getAuthToken = getAuthToken;
    exports.SESSION_DURATION = 1e3 * 60 * 60 * 24 * 30;
    async function getAuthToken(rpcUrl, publicKey, signMessage) {
      const bs58 = (await import("./esm-RLHJW56B.js")).default;
      const challengeResponse = await fetch(`${rpcUrl}/auth/challenge?pubkey=${publicKey.toString()}`);
      const { challenge } = await challengeResponse.json();
      const signature = await signMessage(new Uint8Array(Buffer.from(challenge, "utf-8")));
      const signatureString = bs58.encode(signature);
      const authResponse = await fetch(`${rpcUrl}/auth/login`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          pubkey: publicKey.toString(),
          challenge,
          signature: signatureString
        })
      });
      const authJson = await authResponse.json();
      if (authResponse.status !== 200) {
        throw new Error(`Failed to authenticate: ${authJson.error}`);
      }
      const expiresAt = authJson.expiresAt ?? Date.now() + exports.SESSION_DURATION;
      return { token: authJson.token, expiresAt };
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/access-control/verify.js
var require_verify = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/access-control/verify.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.verifyTeeRpcIntegrity = verifyTeeRpcIntegrity;
    async function verifyTeeRpcIntegrity(rpcUrl) {
      const { default: init, js_get_collateral: jsGetCollateral, js_verify: jsVerify } = await import("./dcap-qvl-web-PMH3FWHC.js");
      const challengeBytes = Buffer.from(Uint8Array.from(Array(32).fill(0).map(() => Math.floor(Math.random() * 256))));
      const challenge = challengeBytes.toString("base64");
      const url = `${rpcUrl}/quote?challenge=${encodeURIComponent(challenge)}`;
      const response = await fetch(url);
      const responseBody = await response.json();
      if (response.status !== 200 || !("quote" in responseBody)) {
        throw new Error(responseBody.error ?? "Failed to get quote");
      }
      await init();
      const rawQuote = Uint8Array.from(Buffer.from(responseBody.quote, "base64"));
      const pccsUrl = "https://pccs.phala.network/tdx/certification/v4";
      const quoteCollateral = await jsGetCollateral(pccsUrl, rawQuote);
      const now = BigInt(Math.floor(Date.now() / 1e3));
      try {
        jsVerify(rawQuote, quoteCollateral, now);
        return true;
      } catch (error) {
        return false;
      }
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/access-control/permission.js
var require_permission = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/access-control/permission.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getPermissionStatus = getPermissionStatus;
    exports.waitUntilPermissionActive = waitUntilPermissionActive;
    async function getPermissionStatus(rpcUrl, publicKey) {
      const [baseUrl, token] = rpcUrl.replace("/?", "?").split("?");
      let url;
      if (token) {
        url = `${baseUrl}/permission?${token}&pubkey=${publicKey.toString()}`;
      } else {
        url = `${baseUrl}/permission?pubkey=${publicKey.toString()}`;
      }
      try {
        const permissionStatusResponse = await fetch(url);
        if (!permissionStatusResponse.ok) {
          throw new Error(`Permission status request failed: ${permissionStatusResponse.statusText}`);
        }
        const response = await permissionStatusResponse.json();
        return response;
      } catch (error) {
        throw new Error(`Failed to get permission status: ${error instanceof Error ? error.message : String(error)}`);
      }
    }
    async function forcePermissionUpdate(rpcUrl, publicKey) {
      const [baseUrl, token] = rpcUrl.replace("/?", "?").split("?");
      let url;
      if (token) {
        url = `${baseUrl}/permission/force-update?${token}&pubkey=${publicKey.toString()}`;
      } else {
        url = `${baseUrl}/permission/force-update?pubkey=${publicKey.toString()}`;
      }
      try {
        const forceUpdateResponse = await fetch(url);
        if (!forceUpdateResponse.ok) {
          throw new Error(`Force permission update request failed: ${forceUpdateResponse.statusText}`);
        }
        return true;
      } catch (error) {
        console.error(`Failed to force permission update: ${error instanceof Error ? error.message : String(error)}`);
        return false;
      }
    }
    async function waitUntilPermissionActive(rpcUrl, publicKey, timeout) {
      const timeoutMs = timeout ?? 5e3;
      let startTime = Date.now();
      while (Date.now() - startTime < timeoutMs) {
        try {
          const { authorizedUsers } = await getPermissionStatus(rpcUrl, publicKey);
          if (authorizedUsers && authorizedUsers.length > 0) {
            return true;
          }
        } catch (error) {
          console.error(error);
        }
        await new Promise((resolve) => {
          setTimeout(resolve, 400);
        });
      }
      const forceUpdateSuccess = await forcePermissionUpdate(rpcUrl, publicKey);
      if (forceUpdateSuccess) {
        startTime = Date.now();
        while (Date.now() - startTime < timeoutMs) {
          try {
            const { authorizedUsers } = await getPermissionStatus(rpcUrl, publicKey);
            if (authorizedUsers && authorizedUsers.length > 0) {
              return true;
            }
          } catch (error) {
            console.error(error);
          }
          await new Promise((resolve) => {
            setTimeout(resolve, 400);
          });
        }
      }
      return false;
    }
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/access-control/index.js
var require_access_control = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/access-control/index.js"(exports) {
    "use strict";
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar(require_auth(), exports);
    __exportStar(require_verify(), exports);
    __exportStar(require_permission(), exports);
  }
});

// node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/index.js
var require_lib = __commonJS({
  "node_modules/@magicblock-labs/ephemeral-rollups-sdk/lib/index.js"(exports) {
    var import_dist = __toESM(require_dist());
    var import_dist2 = __toESM(require_dist2());
    var import_dist3 = __toESM(require_dist3());
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar(require_constants(), exports);
    __exportStar(require_pda(), exports);
    __exportStar(require_utils(), exports);
    __exportStar(require_resolver(), exports);
    __exportStar(require_magic_router(), exports);
    __exportStar(require_instructions(), exports);
    __exportStar(require_access_control(), exports);
  }
});
export default require_lib();
//# sourceMappingURL=@magicblock-labs_ephemeral-rollups-sdk.js.map
